@model IEnumerable<Model.DTO.CourseDTO>
@{
    ViewBag.Title = "Course List";
}
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@inject IHtmlLocalizer<SharedRes> SharedLocalizer
<a asp-action="Create">@SharedLocalizer["Addcourse"]</a>

<table class="table">
    <tr>
        <th>@SharedLocalizer["Coursename"]</th>
        <th>Creation date</th>
    </tr>
    @foreach (var course in Model)
    {
        <tr>
            <td>@course.Name</td>
            <td>@course.CreationDate.ToString("dd/MM/yyyy")</td>
            <td>
                <form asp-action="Delete" asp-route-id="@course.Id" method="post">
                    <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-id="@course.Id">@SharedLocalizer["Edit"]</a>
                    @if (course.IsActive)
                    {
                    <a class="btn btn-sm btn-danger" asp-action="Suspend" asp-route-id="@course.Id">@SharedLocalizer["Suspend"]</a>
                    }
                    else
                    {
                    <a class="btn btn-sm btn-primary" asp-action="Suspend" asp-route-id="@course.Id">@SharedLocalizer["Activate"]</a>
                    }
                </form>
            </td>
        </tr>
    }
</table>